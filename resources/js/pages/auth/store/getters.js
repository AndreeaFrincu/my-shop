export default {
    getNewUser(state) {
        return state.newUser
    },

    getLoginFormUser(state) {
        return state.loginFormUser
    },

    getAuthenticatedUser(state) {
        return state.authenticatedUser
    }
}
